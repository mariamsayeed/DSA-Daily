*Steps to apply insertion sort:*
1. put the elements in its correct position
2. swap adjacent elements while comparing

## TIME COMPLEXITY
    TC -> O(n^2) {worst case and average case}
    TC -> O(n) {best case}
    SC -> O(1)
